<%= form_with(model: mob_spawn_point) do |form| %>
  <% if mob_spawn_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mob_spawn_point.errors.count, "error") %> prohibited this mob_spawn_point from being saved:</h2>

      <ul>
        <% mob_spawn_point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mobid %>
    <%= form.number_field :mobid %>
  </div>

  <div class="field">
    <%= form.label :mobname %>
    <%= form.text_field :mobname %>
  </div>

  <div class="field">
    <%= form.label :polutils_name %>
    <%= form.text_field :polutils_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
